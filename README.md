# Casino Data Process API

API for the Casino DW Admin system.

## Tech Stack
- Node.js
- Express
- Sequelize (ORM)
- SQL Server (MSSQL)

## Setup
1. Install dependencies: `npm install`
2. Configure `.env` file based on `.env.example`
3. Run development server: `npm run dev`
